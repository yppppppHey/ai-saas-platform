server:
  port: 8083

spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace:
        group: DEFAULT_GROUP
      config:
        server-addr: localhost:8848
        namespace:
        group: DEFAULT_GROUP

  datasource:
    url: jdbc:mysql://localhost:3306/yuping?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  redis:
    host: localhost
    port: 6379
    password:
    database: 0

rocketmq:
  name-server: localhost:9876

logging:
  level:
    com.aisaas.task: DEBUG
    com.baomidou.mybatisplus: DEBUG
