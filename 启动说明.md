# AISaaS 微服务平台 — 本地启动说明

> 适用环境：Windows + JDK 17 + Maven 3.6.3。本文记录本项目中**已验证可跑通**的完整启动流程与踩过的坑。

## 1. 环境依赖

| 组件 | 地址/端口 | 账号 | 备注 |
|---|---|---|---|
| MySQL | localhost:3306 | root / 123456 | 数据库 `yuping` |
| Redis | localhost:6379 | 无密码 | 单机模式 |
| Nacos | localhost:8848（gRPC 9848） | 无鉴权 | standalone |
| RocketMQ | namesrv 9876 / broker 10911 | — | 5.1.4 |
| Qdrant | localhost:6334 | — | **可选**，不启动会自动降级为内存向量库 |

服务端口：gateway 8080、user 8081、chat 8082、task 8083、billing 8084、rag 8086。

## 2. 构建

本项目 Maven 位于 `E:/maven/apache-maven-3.6.3-bin/apache-maven-3.6.3`，其 `bin/mvn` 在 Git Bash 下会报
“找不到或无法加载主类 Launcher”，必须用 java 直接启动 classworlds：

```bash
cd e:/AISaaS微服务平台/ai-saas-server-platform-master/ai-saas-server-platform-master

/d/jdk17/bin/java -Xms512m -Xmx1024m \
  -classpath "E:/maven/apache-maven-3.6.3-bin/apache-maven-3.6.3/boot/plexus-classworlds-2.6.0.jar" \
  "-Dclassworlds.conf=E:/maven/apache-maven-3.6.3-bin/apache-maven-3.6.3/bin/m2.conf" \
  "-Dmaven.home=E:/maven/apache-maven-3.6.3-bin/apache-maven-3.6.3" \
  "-Dmaven.multiModuleProjectDirectory=e:/AISaaS微服务平台/ai-saas-server-platform-master/ai-saas-server-platform-master" \
  org.codehaus.plexus.classworlds.launcher.Launcher \
  -o -pl ai-saas-gateway,ai-saas-user-service,ai-saas-chat-service,ai-saas-billing-service,ai-saas-rag-service,ai-saas-task-service -am clean package -DskipTests
```

- `-o` 离线构建（依赖已全部入本地仓库）；**需要联网拉新依赖时去掉 `-o`**。
- **`-am` 必须带**：`ai-saas-common` 没有 install 到本地仓库，单模块构建会报
  `Could not find artifact com.aisaas:ai-saas-common`。
- 全量 clean 构建约 3~5 分钟。

## 3. 启动服务

**必须显式传 `--server.port`**：yml 中配置的端口会被未知因素覆盖成随机端口（61169 之类），
虽仍会带着真实端口注册到 Nacos，但不利于调试，命令行参数可强制固定端口。

```bash
JAR_ROOT=e:/AISaaS微服务平台/ai-saas-server-platform-master/ai-saas-server-platform-master

# 网关最后启动，业务服务可并行拉起（内存紧张时建议逐个启动，每个约 40~70 秒）
/d/jdk17/bin/java -Xms256m -Xmx512m -jar "$JAR_ROOT/ai-saas-gateway/target/ai-saas-gateway-1.0.0-SNAPSHOT.jar"            --server.port=8080 &
/d/jdk17/bin/java -Xms256m -Xmx512m -jar "$JAR_ROOT/ai-saas-user-service/target/ai-saas-user-service-1.0.0-SNAPSHOT.jar"       --server.port=8081 &
/d/jdk17/bin/java -Xms256m -Xmx512m -jar "$JAR_ROOT/ai-saas-chat-service/target/ai-saas-chat-service-1.0.0-SNAPSHOT.jar"       --server.port=8082 &
/d/jdk17/bin/java -Xms256m -Xmx512m -jar "$JAR_ROOT/ai-saas-task-service/target/ai-saas-task-service-1.0.0-SNAPSHOT.jar"       --server.port=8083 &
/d/jdk17/bin/java -Xms256m -Xmx512m -jar "$JAR_ROOT/ai-saas-billing-service/target/ai-saas-billing-service-1.0.0-SNAPSHOT.jar" --server.port=8084 &
/d/jdk17/bin/java -Xms256m -Xmx512m -jar "$JAR_ROOT/ai-saas-rag-service/target/ai-saas-rag-service-1.0.0-SNAPSHOT.jar"        --server.port=8086 &
```

日志建议重定向到 `E:/AISaaS微服务平台/svclogs/<服务名>.log`。

启动成功标志：日志出现 `Started XxxApplication` 与 `nacos registry ... register finished`。

## 4. 健康检查

```bash
for p in 8080 8081 8082 8083 8084 8086; do
  curl -s "http://localhost:$p/actuator/health" | head -c 60; echo " <- $p"
done
```

## 5. 端到端自测（走网关 8080）

```bash
# 1) 发送验证码（返回 data 即 verifyKey）
curl -X POST "http://localhost:8080/api/auth/send-code?target=8001@test.com&type=email"

# 2) 从 user-service 日志取验证码（LoggingVerifyCodeNotifier 会打印）
grep "发送验证码" E:/AISaaS微服务平台/svclogs/user.log | tail -1

# 3) 注册（注意要有请求体，空 body 会触发网关 SecurityFilter 的已知问题）
curl -X POST "http://localhost:8080/api/auth/register" -H "Content-Type: application/json" \
  -d '{"username":"yuping","password":"Test123456","confirmPassword":"Test123456",
       "email":"8001@test.com","verifyCode":"<验证码>","verifyKey":"<上一步的data>"}'

# 4) 登录（字段名是 account，不是 username）
curl -X POST "http://localhost:8080/api/auth/login" -H "Content-Type: application/json" \
  -d '{"account":"yuping","password":"Test123456"}'
```

已建测试账号：`yuping` / `Test123456`。

## 6. 已知坑（重要）

1. **端口必须命令行指定**：见第 3 节，yml 端口会被覆盖成随机端口。
2. **重建前先杀掉相关 java 进程**：Spring Boot 启动失败的 JVM 因非守护线程**不会退出**，会锁住 jar，
   导致 `maven clean` 报 `Failed to delete ... .jar`。用 PowerShell 清理：
   ```powershell
   Get-CimInstance Win32_Process -Filter "Name='java.exe'" |
     Where-Object { $_.CommandLine -like "*ai-saas-*" } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
   ```
3. **数据源密码统一 123456**：`application.yml` 里的默认值也要改（不能只改 `application-dev.yml`，
   task 服务就因为默认密码仍是 `root` 而连不上库）。
4. **RocketMQTemplate 需要 `rocketmq.producer.group`**：只配 `name-server` 时模板 bean 不会生成
   （`ProducerOrConsumerPropertyCondition` 把关），会连累 common 的 `RocketMQTransactionConfig`。
5. **Nacos 配置中心必须显式关闭**：各服务 `spring.cloud.nacos.config.enabled: false`，
   否则报 `No spring.config.import property has been defined`。
6. **网关禁用了 Sentinel 的 SCB 集成**（`spring.cloud.circuitbreaker.sentinel.enabled=false`）：
   否则它会抢占 `ReactiveCircuitBreakerFactory`，导致 resilience4j 工厂退避、路由里的
   `CircuitBreaker` 过滤器注册不出来。
7. **网关已移除全局 RequestRateLimiter**：SCG 4.1.2 在响应提交后往只读 headers 写 `X-RateLimit-*`
   （Tomcat reactive 下必抛 `UnsupportedOperationException`），会干扰熔断器误触发兜底。
8. **网关路由路径已对齐真实控制器**：user 是 `/api/auth`、`/api/user` 等（**没有** `/api/v1` 前缀），
   而 chat/task/rag 用的是 `/api/v1/**`——改动路由或新增接口时需留意这个不一致。
9. SQL 变更以 `ai-saas-chat-service/src/main/resources/db/chat_init.sql` 和
   `docker/mysql/init/02-chat.sql` 为准（两者需同步），`sql/init.sql` 已废弃删除。

## 7. 停止服务

```powershell
Get-CimInstance Win32_Process -Filter "Name='java.exe'" |
  Where-Object { $_.CommandLine -like "*ai-saas-*" } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```
